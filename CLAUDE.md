# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Purpose

TravelGuide is a menu-driven CLI application that crawls georgiantravelguide.com and stores attraction data in a database, to be used for composing motorcycle routes. Crawl tasks (a name plus start-point URLs) are database entities; each task's submenu has a "Run this task" command (`RunTaskCommand`) that harvests attraction URLs (from the sitemap over HTTP, or from the listing page with Selenium — user's choice at run start) and extracts per-page data over plain HTTP into the `Places` table (see "Task pattern" below).

Code comments are written in Georgian; the user typically communicates in Georgian.

## Critical: External Sibling Dependencies

`TravelGuide.slnx` references ~25 shared library projects that live OUTSIDE this repository, as sibling directories of the repo root (`D:\1WorkDotnet\TravelGuide\`): `AppCliTools`, `ConnectionTools`, `DatabaseTools`, `ParametersManagement`, `SystemTools`, `ToolsManagement`, `WebAgentContracts`, plus the two repositories split out of this one: `TravelGuideCore` (`TravelGuideCore.Domain` entities + `TravelGuideCore.Application.Abstractions` with `ITravelGuideApplicationDbContext`) and `TravelGuideDbPart` (`TravelGuideDbPart.Db` — `TravelGuideDbContext` + entity configurations). They must be checked out side-by-side or the solution does not build. Key ones:

- **AppCliTools.CliTools / CliMenu** — the CLI app loop (`CliAppLoop`), menu framework (`CliMenuSet`, `CliMenuCommand`), menu-builder services
- **ParametersManagement.LibParameters** — JSON parameters-file management (`ParametersManager`, `IParametersManager`, `ParametersEditor`)
- **SystemTools.SystemToolsShared** — common helpers (`StShared`, `ItemData`, `Pluralize()`, `UnCapitalize()`)
- **SystemTools.DatabaseToolsShared** — `SqlServerDesignTimeDbContextFactory` base for EF design-time support

## Commands

Build (from repo root):

    dotnet build TravelGuide.slnx

Run (a parameters JSON file is mandatory; it lives outside the repo — see `TravelGuide/Properties/launchSettings.json`):

    dotnet run --project TravelGuide -- --use "D:\1WorkSecurity\TravelGuide\TravelGuide.json"

EF Core migrations do not live in this repository: since 2026-09-15 they are in the sibling repository `TravelGuideDbTools` (`D:\1WorkDotnet\TravelGuideDbTools\TravelGuideDbTools`, github.com/merabza/TravelGuideDbTools) — `TravelGuideDbTools.DbMigration` is the migrations assembly and `TravelGuideDbTools.FakeHost` the startup project whose only purpose is design-time `dotnet ef` support. Run the commands from that repository:

    dotnet ef migrations add <Name> --project TravelGuideDbTools.DbMigration --startup-project TravelGuideDbTools.FakeHost
    dotnet ef database update --project TravelGuideDbTools.DbMigration --startup-project TravelGuideDbTools.FakeHost

The design-time factory there (`TravelGuideDbTools.FakeHost/TravelGuideDesignTimeDbContextFactory.cs`) reads the connection string from User Secrets key `ConnectionString` (`UserSecretsId` is set in `TravelGuideDbTools.FakeHost.csproj`; set the secret with `dotnet user-secrets set ConnectionString "..."` there). Migrations so far: `20260916032014_Initial` (creates the whole schema, `Places.Url` / `UrlHashCode` already nullable), `20260916064238_UrlsTable` (moves the URL into the new `Urls` table — see `PlaceModel` below; its `Up()` / `Down()` are hand-written to keep data: `Urls` rows are inserted with `UrlId` = the old `PlaceId`, so the `UrlGraphNodes` ids stay valid, and it throws instead of dropping anything when a place has a `Url` without `UrlHashCode` or a graph edge points at a URL-less place) and `20260916082353_MoveStateToUrls` (moves the crawl state from `Places.State` to `Urls.State` — hand-written as well: the new column is added first, the values are copied over the `Places.UrlId` join and only then the old column is dropped; it throws when two places sharing one URL carry different states; `Down()` copies the values back and gives URL-less places `Analysed`, the former editor default) — run `dotnet ef database update` against the target database before the task menu commands can actually persist anything. For an existing database the same restructure is available as plain T-SQL: `SqlScripts/MigratePlaceUrlsToUrlsTable.sql` (numbered steps, orphan checks first, the schema switch in one transaction, and finally the migration is registered in `__EFMigrationsHistory` so `database update` does not re-apply it).

There are no test projects.

## Build Strictness

`Directory.Build.props` applies to all projects: `net10.0`, `TreatWarningsAsErrors=true`, `AnalysisMode=All`, SonarAnalyzer.CSharp injected everywhere — any analyzer warning fails the build, so new code must be fully analyzer-clean. `ImplicitUsings` is disabled solution-wide (explicit `using System;` etc. required); a few projects (DbMigration, DbPersistence, FakeHost) re-enable it locally. NuGet versions are centrally managed in `Directory.Packages.props` — `PackageReference` entries in csproj files carry no `Version`.

`.editorconfig` enforces (as errors): file-scoped namespaces, `using` directives outside namespace, braces always, CRLF, 4-space indent. Classes are `sealed` by convention. `//Created by ...Creator` headers mark template-generated files; commented-out blocks (e.g. all of `TravelGuide/TravelGuide.cs`, parts of `MenuData.cs`) are intentional scaffolding examples — leave them unless asked.

## Architecture

Startup flow in `TravelGuide/Program.cs`: parse `--use <parameters.json>` via `ArgumentsParser<TravelGuideParameters>` → build DI container (`TravelGuide/DependencyInjection/TravelGuideServices.cs`) → `CliAppLoop.Run()` renders the main menu built by `TravelGuideMenuBuilder`.

Projects (dependency order):

- **DoTravelGuide** — `TravelGuideParameters`: the persisted app configuration (LogFolder, DatabaseServerConnections, DatabaseParameters, ApiClients, SmartSchemas)
- **TravelGuideCore.Domain** (repo `TravelGuideCore`; one folder per entity, folder = pluralized class name, e.g. `PlaceModels/PlaceModel.cs` in namespace `TravelGuideCore.Domain.PlaceModels`, `PlacesByTags/PlaceByTag.cs`; `EState` sits in `UrlModels/` because since 2026-09-16 the state belongs to the URL, `StringExtension` at the project root) — EF entity classes: `TaskModel` (unique `TaskName`, max 50) with a collection of child `TaskStartPoint`s (`StartPoint`, max 2048); `PlaceModel` (`ItemData`-derived so the Places editor's field editors can work on it; global crawled attraction with an optional `UrlId` → `Urls` plus `UrlNavigation` (both `init`-only: assigned once at creation, never changed) — since 2026-09-16 the URL is its own entity `UrlModel` (`UrlModels/UrlModel.cs`, table `Urls`, PK `UrlId`, required `Url` max 500 (`UrlModelConfiguration.UrlLength`) plus its deterministic hash `UrlHashCode` — `StringExtension.GetDeterministicHashCode`, same algorithm as the Crawler sibling project; the non-unique hash index is the only index, `Url` itself is not indexed, and app code enforces Url uniqueness before insert by hash lookup + exact compare), because a place may have no URL at all while the page-link graph is about URLs: a place entered by hand in the Places editor has no `UrlId`, and the crawler never touches such rows (`GetPlacesForAnalysis` skips them); the crawler always creates a `Urls` row together with its `New` place (through `UrlNavigation`, one save), and deleting a place in the editor deletes its `Urls` row (and that row's graph edges) with it, so a re-crawl rediscovers the URL exactly as before the split; the crawl state is the URL's, not the place's: `UrlModel.State` (`EState`, moved from `Places.State` to `Urls.State` on 2026-09-16 by the `MoveStateToUrls` migration; only `New`/`Analysed`/`NotAttraction`/`DownloadError`/`Duplicate` are persisted, stored as int — new members append at the enum's end), so a URL-less place has no state at all); distances are normalized: `FromPointModel` (`FromPoints`, unique dynamically-created `Name` like „თბილისიდან", plus an optional `LocationId` → `Locations` with a `LocationNavigation` — one shared location per from-point, filled only through the FromPoints editor; the crawler creates from-points by name only) + `DistanceByPlace` (`DistanceByPlaces`, own PK, unique `(PlaceId, FromPointId)`, `Distance` in km); locations are normalized too — a place may hold several and a pair may be shared between places: `LocationModel` (`Locations`, unique `(Latitude, Longitude)`, coordinates as `double` — the shared convention maps `decimal` to `money` which would truncate them) + `PlaceByLocation` (`PlacesByLocations`, composite PK `(PlaceId, LocationId)`, `PlaceByTag`-shaped join); `UrlGraphNode` (`UrlGraphNodes`, PK `UgnId`, unique `(FromUrlId, GotUrlId)` — the page-link graph: on the page of which URL which URL was found; both FKs point to `Urls` (until 2026-09-16 to `Places`) with `DeleteBehavior.Restrict` because SQL Server rejects two cascade paths, and no navigation properties — rows are created from raw `UrlId`s); `RouteDistanceModel` (`RouteDistances`, PK `RouteDistanceId`, `StartLocationId` / `EndLocationId` both FK → `Locations` with `DeleteBehavior.Restrict` for the same two-cascade-paths reason and no navigations, unique `(StartLocationId, EndLocationId)`; `AirDistance` / `RoadDistance` in km + `RoadTime`, settable because a re-run corrects rows in place; the start is a from-point's location, the end a place location); `VisitModel` (`Visits`, PK `VisitId`, `LocationId` FK → `Locations` — a visit marks one location, so one visited location of a multi-location place leaves its other locations unvisited; `MotorcycleId` FK → `Motorcycles`, `VisitDate`, optional `Comment`, child `VisitImage`s; no navigations to the parents. Until 2026-09-11 the FK was `PlaceId` → `Places`; `SqlScripts/MigrateVisitsToLocations.sql` moves existing rows to the new shape on the transfer source before the schema sync — single-location places map automatically, the rest are listed for a manual pick or deleted)
- **TravelGuideCore.Application.Abstractions** (repo `TravelGuideCore`) — `ITravelGuideApplicationDbContext`: every `DbSet` plus `SaveChanges` / `BeginTransaction` / `Update<TEntity>`; the repository layer depends on this interface, never on the concrete context
- **TravelGuideDbPart.Db** (repo `TravelGuideDbPart`) — `TravelGuideDbContext` (implements `ITravelGuideApplicationDbContext`); per-entity `IEntityTypeConfiguration` classes in `Configurations/` set table names (`Tasks`, `TaskStartPoints`), keys, the unique `TaskName` index and max lengths; a shared `DatabaseEntitiesDefaultConvention` (SystemTools.DatabaseToolsShared) added in `ConfigureConventions` supplies default table/column naming
- **TravelGuideDbTools.DbMigration / TravelGuideDbTools.FakeHost** (repo `TravelGuideDbTools`, not part of `TravelGuide.slnx`) — migrations assembly (`20260916032014_Initial` creates the whole schema, `20260916064238_UrlsTable` moves the URLs into the `Urls` table without data loss, `20260916082353_MoveStateToUrls` moves the crawl state from `Places` to `Urls` without data loss) and the minimal web host used only as `dotnet ef` startup project
- **TravelGuideRepoInterfaces / TravelGuideRepositories** — repository layer over the DbContext: `ITravelGuideRepository` (Task / TaskStartPoint CRUD, Place methods `AddPlace` (a new place may carry a new `UrlNavigation`, inserted with it) / `GetPlacesForAnalysis` (rows with a `UrlNavigation` only, filtered on `UrlNavigation.State` — hand-entered places have no state and are never downloaded) / `HasAnalysedPlaces` / `HasDownloadErrorPlaces` (both look at `Urls.State`) / `GetNearestPlaces` (returns `PlaceByLocation` links — a multi-location place yields one row per location, ordered by the `EOrderVisitsBy` argument (the `OrderVisitsBy` parameter; `RecommendedVisitsCommand` falls back to `RoadTime` when it is unset): road time or road distance taken from the `RouteDistances` row keyed by the start location's id and the link's `LocationId` (the start point is passed as a `LocationModel`: the location of the FromPoint that `RecommendedVisitsCommand.RunBody` asks the user to pick on entry with `SelectFromListInput`, listing only from-points that have a location, before the min-road-time and max-visits questions; the former `MyPlaces` / `MyCurrentPlaceName` parameters and their editors were removed) — rows without a computed route go last and air distance breaks ties — or the cos-corrected squared-degree air distance; then `PlaceId`, `LocationId` for stable paging; the min-road-time filter also applies per location; the max-visits-count filter applies per location (visit count ≤ requested maximum — `Visits.LocationId` references `Locations`, so visiting one location of a multi-location place leaves its other locations unvisited); the URL state is deliberately not considered — since 2026-09-16 `Duplicate` places are no longer excluded) / `GetPlaceLinkedLocations` (the distinct `Locations` rows referenced by at least one `PlacesByLocations` row, no-tracking — the FromPoints editor's Calculate Distances counts a route to each of them, orphan locations are skipped) / place-location link methods `GetPlaceLocations` (one place's links with `LocationNavigation`, no-tracking, ordered by `LocationId`) / `GetPlaceLocation` (tracked, by `(PlaceId, LocationId)`) / `AddPlaceLocation` (place id + `LocationModel`, which may be a not-yet-saved `GetOrCreateLocation` result — the link is written through the navigation) / `DeletePlaceLocation`
, Places-editor methods `GetPlacesCount` / `GetPlacesPortion` (optional name-or-url substring filter, ordered by `Name ?? UrlNavigation.Url` then `PlaceId`, skip/take, no-tracking copies with `UrlNavigation`) / `GetPlaceById` (tracked, with `UrlNavigation`) / `UpdatePlace` / `DeletePlace`, lookup lists `GetRegionsList` / `GetMunicipalitiesList` (no-tracking, by name), lookup-editor methods `GetRegionByName` (no-tracking, DB-side compare) / `GetRegionById` (tracked) / `UpdateRegion` / `DeleteRegion` / `GetPlacesCountByRegionId` plus their Municipality / Category / Tag / FromPoint twins (the latter three also got `GetCategoriesList` / `GetTagsList` / `GetFromPointsList`; `GetFromPointsList` includes `LocationNavigation`), lookup `GetOrCreate*` helpers (Category/Tag/FromPoint/Location/Region/Municipality; `GetOrCreateLocation` matches both doubles exactly — intentional, the unique index is exact too), Url methods `GetUrlIdsByUrlHashCode` (url→`UrlId` of `Urls` rows sharing the given hash; the caller exact-compares `Url`) / `DeleteUrl`, UrlGraphNode methods `AddUrlGraphNode` / `GetAllUrlGraphNodes` / `DeleteUrlGraphNodesByUrlId` (both directions, tracked removal — the Places editor runs it before `DeleteUrl` because both FKs are Restrict), RouteDistance methods `AddRouteDistance` / `GetRouteDistance(startLocationId, endLocationId)` / `GetRouteDistancesByStartLocationId` (tracked — Calculate Distances corrects rows in place), plus SaveChanges / transaction helpers), `ITravelGuideRepositoryCreatorFactory` (resolves a repository from the root provider — safe because `Program.cs` builds the ServiceProvider without scope validation)
- **TravelGuide** — the executable: DI, menu, menu commands (`MenuCommands/`)

Database wiring (`TravelGuideServices.AddDatabase`) resolves provider + connection string from the parameters file via `DbConnectionFactory`; only SQL Server is currently wired to `TravelGuideDbContext`, and `ITravelGuideApplicationDbContext` is registered as a scoped forward to that context (`AddScoped<ITravelGuideApplicationDbContext>(sp => sp.GetRequiredService<TravelGuideDbContext>())`).

### Menu system

The main menu is assembled by `CliMenuSetFactory.CreateMenuSet("Main Menu", MenuData.MenuCommandNames, ...)`. To add a main-menu item:

1. Create a class implementing `IMenuCommandFactoryStrategy` that builds a `CliMenuCommand` (examples: `TravelGuide/Menu/TravelGuideParametersEdit/TravelGuideParametersEditorListCliMenuCommandFactoryStrategy.cs`, `TravelGuide/MenuCommands/NewTaskCommandFactoryStrategy.cs`), or — for a dynamic group of items — `IMenuCommandListFactoryStrategy` returning one `CliMenuCommand` per item (example: `TravelGuide/MenuCommands/TasksListFactoryStrategy.cs`). The class must be `public` — the DI scanner only sees exported types.
2. Add its name (`nameof(...)`) to `MenuData.MenuCommandNames` (`TravelGuide/Menu/MenuData.cs`). `CliMenuSetFactory` looks the name up among singular strategies first, then list strategies; unmatched names are silently skipped.

Strategies are auto-registered in DI by `AddTransientAllStrategies<IMenuCommandFactoryStrategy>` / `AddTransientAllStrategies<IMenuCommandListFactoryStrategy>` scanning the TravelGuide assembly. Parameters editing UI is `TravelGuideParametersEditor` (field editors per parameter).

The main-menu `Lists` submenu (`ListsSubMenuCommand`) holds DB-backed table editors built on AppCliTools' `Cruder`: `MotorcycleCruder` (a plain `CruderListCliMenuCommand`, record key = `MotorcycleKey`) and the Places editor. `Places` has thousands of rows, so it is not a plain cruder list: `PlacesCommand` asks for an optional name-or-url filter in `RunBody` (Enter = no filter), then `GetSubMenu` shows `New Place` plus one DB-loaded portion (`PlaceCruder.LoadPortion` → `GetPlacesPortion`; portion size from the console height, PageUp/PageDown via the Visits menu's `ChangePortionCommand`) of `PlaceSubMenuCommand` items (name = the caption from `PlaceModelExtensions.GetCaption` (`Menu/PlaceModelExtensions.cs`, also used by `RecommendedPlaceSubMenuCommand`): place `Name`, or `Url` when unnamed, or `Place {PlaceId}` when it has neither; duplicate captions get a ` (N)` suffix; status = `State | Url` of the place's URL, none for a URL-less place). `PlaceCruder` (`fieldKeyFromItem=true`, record key = that caption — unique within the portion, like `VisitCruder`'s; until 2026-09-15 the key was `Url`) keeps only the last loaded portion as its cruder dictionary — the record menu (Delete / Edit all fields in sequence / read-only `Url` item, present only when the place has a `UrlNavigation` / field editors) works on those no-tracking copies, so `UpdateRecordWithKey` re-fetches the tracked row by id (`GetPlaceById`) and copies `Name`, `Description`, `RegionId`, `MunicipalityId` and, when the place has a URL, `UrlNavigation.State`. Field editors: `OptionalTextFieldEditor` for `Name`, `DescriptionFieldEditor` (single-line input; status collapsed to one line and truncated to the console width — a huge Table-view status would shrink the field-name column to nothing), `PlaceStateFieldEditor` (works on `UrlNavigation.State` instead of a reflected property because the state belongs to the URL: persisted states only, default `Analysed` for a new place with a URL so the crawler leaves it alone, and a no-op with an empty status for a URL-less place), `LookupIdFieldEditor` for `Region`/`Municipality` (pick from the lookup table or `(None)`; explicit menu ids so Enter keeps the current value). The `Url` is asked once in `CreateNewItem` and never edited afterwards; it is optional — an empty Enter creates a place without a `Urls` row (one that does not exist on the site, entered by hand, which the crawler never touches), a given value is trimmed of trailing `/`, checked against `UrlModelConfiguration.UrlLength` and hash-based uniqueness in `Urls` like `HarvestedUrlPersister`, and becomes a new `UrlModel` assigned to `UrlNavigation` (saved together with the place). Deleting refuses a place whose locations have Visits (visits reference `Locations`; deleting the place would leave them on a location no place links to) and, when the place has a URL, removes its `UrlGraphNodes` edges and its `Urls` row together with it. The record menu's last field item, `Locations` (`PlaceLocationsFieldEditor`, a sub-object field editor on `PlaceModel.Locations`; its status — `No Locations` / the single `lat, lon` / `N Locations` — and its list are loaded from the DB by `PlaceId` because the portion copies carry no links), opens `PlaceLocationCruder`, a `VisitImageCruder`-style child editor over `PlacesByLocations`: record key = `lat, lon` (InvariantCulture, `LocationItem.GetItemKey`; the `ItemData` copy also carries the link's `LocationId`); `New Location` asks Latitude/Longitude via `DoubleFieldEditor` and refuses pairs outside world bounds (reuses `PlaceDataExtractor.IsValidCoordinatePair`, now `internal`) or already linked to the place; add = `GetOrCreateLocation` + `AddPlaceLocation` (locations are shared rows, so an existing pair is reused); the item menu offers Delete, `Edit All fields in sequence` and the `Latitude` / `Longitude` field editors — a single-field edit changes the key, which AppCliTools' `FieldEditorMenuCliMenuCommand` detects after `Save` via `Cruder.CheckRecordKeyChanged` (only for `fieldKeyFromItem` cruders whose item overrides `GetItemKey()`; it bumps the cruder's list-menu version and switches the success action from `Reload` to `LevelUp`), so the editor returns to the Locations list, which is reloaded from the DB; edit = unlink old + link new (`UpdateRecordWithKey`, no-op when the key is unchanged), delete = unlink only — orphaned `Locations` rows stay, like `SyncLocations`. `Regions`, `Municipalities`, `Categories`, `Tags` and `FromPoints` are plain `CruderListCliMenuCommand`s over `RegionCruder` / `MunicipalityCruder` / `CategoryCruder` / `TagCruder` / `FromPointCruder`, thin subclasses of the abstract `LookupCruder` (`Menu/Lists/LookupCruder.cs`; record = `LookupItem` no-tracking copy with `Id` + `Name`, key = `Name`, `fieldKeyFromItem=true`, one `TextFieldEditor` for `Name`; the persistence hooks `Create(item, name)` / `Update(item, name)` receive the validated, trimmed name plus the item so a subclass with extra fields can save them, and `UpdateRecordWithKey` always saves — the field-editor command calls it after every field, and a non-key field may have changed): the item menu offers Delete, `Edit All fields in sequence` and the framework's `Name` field editor (Table status like the Motorcycles fields) — a rename changes the key, so the key-change-aware `FieldEditorMenuCliMenuCommand` rebuilds the list menu and returns `LevelUp` to it (see the Locations editor above); rename is an in-place `Name` update of the tracked row by id (`GetRegionById` + `UpdateRegion`) so `Places.RegionId` references survive; names are trimmed, length-checked against the configuration constant and checked for uniqueness with a DB-side compare (`GetRegionByName`, collation-aware like the unique index); create reuses the crawler's `GetOrCreateRegion`; Delete refuses an entry still referenced by places (`GetPlacesCountByRegionId`; for Categories / Tags / FromPoints the count is over the junction rows `PlacesByCategories` / `PlacesByTags` / `DistanceByPlaces`, whose required FKs would otherwise cascade-delete the links) — Region / Municipality can be reassigned in the Places editor, the other links only change through a re-crawl. Another `{Id, Name}` lookup editor = one more thin `LookupCruder` subclass plus the same six repository methods (list / by-name / by-id / update / delete / usage count). `FromPointCruder` is the lookup with an extra field: record = `FromPointItem` (`LookupItem` subclass — `LookupItem` is therefore not sealed — with `LocationItem? Location`, built from `GetFromPointsList`), a second field editor `OptionalLocationFieldEditor` (`Menu/Lists/`, `FieldEditor<LocationItem?>`; asks Latitude then Longitude in one step because the record menu reloads from the DB after every field edit, so two separate editors could never get a half-entered pair saved; an empty Latitude — Enter when there is no location, or Delete + confirm on an existing one — clears the location, Longitude is required once a Latitude was given, pairs outside world bounds are re-asked with the entered values as defaults via `PlaceDataExtractor.IsValidCoordinatePair`; status `lat, lon` / `No Location`), a `CreateNewItem` override, and `Create`/`Update` hooks that link the location through `SetLocation`: `GetOrCreateLocation` assigned to `LocationNavigation` (shared `Locations` rows, unsaved ones allowed), clearing sets both `LocationId` and the navigation to null because the navigation may not be loaded; orphaned `Locations` rows stay, like everywhere else. The FromPoint record menu's last item is `Calculate Distances` (`Menu/Distances/CalculateDistancesCommand`, added by `FromPointCruder.FillDetailsSubMenu`, which is why `FromPointCruder`, `ListsSubMenuCommand` and its factory strategy take an `IHttpClientFactory`): from the from-point's location to every place-linked location (`GetPlaceLinkedLocations`) it computes the haversine air distance and the OSRM driving route (`DistanceCounter`, 500 ms between requests, 10 s timeout) and stores one `RouteDistances` row per `(StartLocationId, EndLocationId)`, saving per pair so an interrupted run keeps its results; the run always starts with a re-calculate question (default no): rows already present for that start location are skipped on no and compared and corrected in place on yes; a place location that is the from-point's own location (same `Locations` row — coordinates are unique, so equal coordinates mean the same row) is never routed to itself and counts as skipped whatever the answer; a from-point without a location gets an error instead. This replaced the former main-menu `Calculate Distances` item, which started from the coordinates of the since-removed `MyCurrentPlaceName` parameter.


### Task pattern (intended crawler workflow)

Tasks are database entities: `TaskModel` plus child `TaskStartPoint`s, managed through `ITravelGuideRepository` (`GetTasksList` / `GetTaskByName` / `CreateTask` / `AddStartPoint` / `SaveChanges`). The main menu's `New Task` item (`TravelGuide/MenuCommands/NewTaskCommand.cs`, wired by `NewTaskCommandFactoryStrategy`) asks for a task name and a start point, checks name uniqueness, and creates a `TaskModel` with one `TaskStartPoint` in a single save. The repository is obtained lazily via `ITravelGuideRepositoryCreatorFactory` inside `RunBody`/`GetSubMenu`/`CreateMenuCommandsList`, so a missing DB configuration surfaces as a caught runtime error instead of a startup crash (if the database hasn't been migrated yet, saves fail gracefully with "Invalid object name 'Tasks'").

Below `New Task`, the main menu lists every task from the DB (`TasksListFactoryStrategy` → one `TaskSubMenuCommand` per task, mirroring the Crawler/CrawlerConsole sibling projects). The per-task submenu offers `Run this task`, `Delete Task`, `Edit Task Name`, `New Start Point`, plus one `StartPointSubMenuCommand` per start point (edit / delete). Convention: destructive/renaming commands return `EMenuAction.LevelUp` (their submenu's identity key is stale), additive ones `EMenuAction.Reload`; menus are rebuilt from the DB on every reload, so lists refresh automatically. `TasksListFactoryStrategy.CreateMenuCommandsList` and `TaskSubMenuCommand.GetSubMenu` must never throw or return null (they run outside `CliMenuCommand.Run`'s exception handling — a throw kills the app), hence their try/catch-and-continue shape.

### Crawler run ("Run this task")

`RunTaskCommand.RunBody` (async) asks up front which way to harvest URLs — sitemap (default) or Selenium listing walk — when the DB already holds `Analysed` places, whether to re-process them, and — only when re-processing was declined and `DownloadError` rows exist — whether to retry those (a "yes" to re-processing retries them implicitly); it also creates the shared `HttpClient` (gzip/brotli decompression, `TravelGuideBot/1.0` UA, `CheckCertificateRevocationList` — CA5399). The whole run is a DB-backed queue (modeled on CrawlerService's `BatchPartRunner`): `EState.New` is the "to download" status of a `Urls` row (the state lives on the URL, not on the place), and every persist goes through one shared `HarvestedUrlPersister` instance which trims trailing `/`, dedups per run via `HashSet`, checks every first-seen URL against the `Urls` table via `GetUrlIdsByUrlHashCode` (same-hash rows fetched, exact `Url` compare — `Url` itself carries no index), and **keeps only URLs "like a start point"** — exactly equal to one of the task's start points or under one of them as a path prefix (so the start point value controls crawl scope). When the caller passes the optional source-page URL, the persister also fills `UrlGraphNodes` (which URL was found on which page): pairs are deduped in memory against a once-loaded set, self-links are skipped, and only URLs that exist as `Urls` rows can be edge ends (`UrlId`s come from the run's url→id cache; the source page's id falls back to a hash lookup) — the analyser passes each analysed page, the Selenium walk passes its start point, while seeding and sitemap harvesting pass nothing (no source page, so no edges). Phase 1 seeds the start points themselves as `New` `Urls` rows each with a place (a new URL is always persisted as a `UrlModel` in state `New` plus a `PlaceModel` pointing at it through `UrlNavigation`, in one save), then harvests: sitemap mode (`SitemapUrlHarvester`, no browser) fetches `/sitemap.xml` from the first start point's host and picks the `_ka` sitemap (~4400 URLs, includes non-attraction pages); Selenium mode spins up a fresh `ChromeDriver` per start point and `GeorgianTravelGuideRunner.Run()` walks the listing UI (hard-coded Georgian-caption clicks, infinite-scroll until the URL count stalls), persisting after every scroll batch so an interrupted scroll loses nothing. Phase 2 (`PlaceAnalyser`, browser-free) loops: load a portion via `GetPlacesForAnalysis` (minus PlaceIds already attempted this run), fetch each page with `HttpClient`, parse once with AngleSharp, harvest the page's links via `PageLinkExtractor` (absolutized, http(s)-only, fragment stripped, query-string URLs dropped — search/filter pages would multiply endlessly) into the same persister, then extract data with `PlaceDataExtractor.Extract(document)`; the loop re-queries until no unattempted places remain, so link-discovered pages are processed in the same run, and a cancellation check per page allows clean interruption. JSON-LD is the primary extraction source (`TouristAttraction` name, `BreadcrumbList` region/municipality) with visible-DOM selector fallbacks; coordinates are the exception — both sources are always read and united, because JSON-LD `geo` carries only the *first* location (full precision) while the visible technical-details list carries *all* locations rounded to 6 decimals: each pair is validated against world bounds (lat [-90,90], lon [-180,180] — UTM-style junk like `297267, 4768381` is dropped) and DOM pairs within 1e-5° of an already-taken pair are skipped so the full-precision JSON-LD value wins; the valid pairs become `Locations` rows via `GetOrCreateLocation` and are linked through `PlacesByLocations` by `PlaceLinksSynchronizer.SyncLocations` (diff-based like the other link collections). AngleSharp has no `InnerText`, so `Description` is assembled by a block-aware text walker (source newlines collapse to spaces, block tags become line breaks). Pages without a `TouristAttraction` JSON-LD node (cities/regions/shops — but still link sources) get their URL marked `EState.NotAttraction` and are permanently excluded by `GetPlacesForAnalysis`; a page whose request `HttpClient` silently auto-redirected to a different URL (trailing-slash-only differences don't count; detected via `response.RequestMessage.RequestUri`) gets its URL marked `EState.Duplicate` without parsing — the final URL is queued as a `New` row (analysed later in the same run) with a `UrlGraphNodes` edge old→canonical, and `Duplicate` URLs are permanently excluded from analysis (`GetNearestPlaces` no longer filters them out); any other failure (HTTP error, exception, missing name) marks the URL `EState.DownloadError` — unless cancellation was requested, in which case the analyser exits leaving the row untouched — and such rows are excluded from later runs until the user answers yes to the retry question; the "re-process analysed" answer applies to the first portion only, the retry-errors answer to the whole run. The tracked entity is mutated only after a fully successful extraction (a shared root-scoped DbContext means half-mutated entities would leak into the next `SaveChanges`) — link harvesting for a page therefore runs *before* any mutation — then `UrlNavigation.State=Analysed` + per-place `SaveChanges`, so an interrupted run resumes naturally.
